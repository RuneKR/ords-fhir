import {Values}     from    '../values';

// create the set
export const quantityComparator: Values = {};

quantityComparator['<'] = true;
quantityComparator['<='] = true;
quantityComparator['>='] = true;
quantityComparator['>'] = true;
